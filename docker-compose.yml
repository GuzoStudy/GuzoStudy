version: "3.8"
services:
  frontend:
    build: ./frontend
    container_name: frontend_c
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: backend_c
    ports:
      - "5000:5000"
    environment:
      - MONGO_URL=${MONGO_URL}
